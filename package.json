{
  "name": "scalable-url-shortener",
  "version": "1.0.0",
  "description": "A horizontally-scalable, cache-first URL shortener designed to sustain 10,000+ requests/sec on commodity hardware, using patterns from large-scale distributed systems (sharded ID generation, cache-aside, multi-process horizontal scaling, backpressure via rate limiting, and Prometheus observability).",
  "main": "src/cluster.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/cluster.js",
    "start:single": "node src/server.js",
    "test": "node --test test/*.test.js",
    "benchmark": "node benchmark/load-test.js"
  },
  "keywords": ["system-design", "url-shortener", "high-throughput", "distributed-systems", "node", "fastify"],
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.28.1",
    "prom-client": "^15.1.3",
    "lru-cache": "^10.4.3"
  },
  "devDependencies": {
    "autocannon": "^7.15.0"
  },
  "engines": {
    "node": ">=18"
  }
}
